import data from './files/data-1.json'

// переменная для результата
const result = data


// ожидаемый результат
const expectResult = {
    userName: "Иван",
    userSurname: "Дмитрачков"
}

export default {
    result,
    expectResult
}